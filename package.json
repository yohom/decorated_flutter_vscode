{
  "name": "decorated-flutter",
  "displayName": "Decorated Flutter",
  "description": "DecoratedFlutter脚手架",
  "version": "0.0.1",
  "engines": {
    "vscode": "^1.80.0"
  },
  "categories": [
    "Other"
  ],
  "activationEvents": [],
  "main": "./out/extension.js",
  "contributes": {
    "commands": [
      {
        "command": "decorated-flutter.feat_gen",
        "title": "Decorated Flutter: Feat Gen(生成新Feature页面模板)"
      },
      {
        "command": "decorated-flutter.feat_rename",
        "title": "Decorated Flutter: Feat Rename(重命名Feature页面)"
      },
      {
        "command": "decorated-flutter.feat_remove",
        "title": "Decorated Flutter: Feat Remove(删除Feature页面)"
      }
    ]
  },
  "scripts": {
    "vscode:prepublish": "pnpm run compile",
    "compile": "tsc -p ./",
    "watch": "tsc -watch -p ./",
    "pretest": "pnpm run compile && pnpm run lint",
    "lint": "eslint src --ext ts",
    "test": "node ./out/test/runTest.js"
  },
  "devDependencies": {
    "@types/glob": "^8.1.0",
    "@types/mocha": "^10.0.1",
    "@types/node": "16.x",
    "@types/vscode": "^1.80.0",
    "@typescript-eslint/eslint-plugin": "^5.53.0",
    "@typescript-eslint/parser": "^5.53.0",
    "@vscode/test-electron": "^2.2.3",
    "eslint": "^8.34.0",
    "glob": "^8.1.0",
    "mocha": "^10.2.0",
    "typescript": "^4.9.5"
  },
  "dependencies": {
    "change-case": "^4.1.2",
    "yaml": "^2.3.1"
  }
}